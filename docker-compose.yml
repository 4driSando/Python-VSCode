version: '3.8'

services:
  python-dev:
    build: .
    container_name: python_emv_dev
    volumes:
      - ./app:/workspace
    working_dir: /workspace
    command: tail -f /dev/null
    networks:
      - dev-network

networks:
  dev-network:
    driver: bridge
